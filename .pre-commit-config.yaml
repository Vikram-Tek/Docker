repos:
  - repo: https://github.com/zricethezav/gitleaks
    rev: v8.30.0   # <- pin to a specific gitleaks release; update as needed
    hooks:
      - id: gitleaks
        args: [--staged, --redact]
